import { StudioTone } from "@/components/StudioMemeCard";

export const REELS_POLL_INTERVAL_MS = 3000;
export const MAX_REEL_REF_IMAGES = 3;
export const TONES: StudioTone[] = ["roast", "funny", "sweet", "bold"];
export const HISTORY_DEFAULT_SHOW = 4;

export const CAROUSEL_FORMATS = ["teach", "story", "authority"] as const;
export const REEL_GOALS = ["engagement", "promotion", "brand_humour"] as const;
